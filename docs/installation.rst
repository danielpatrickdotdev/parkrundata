============
Installation
============

At the command line::

    $ easy_install parkrundata

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv parkrundata
    $ pip install parkrundata
